import { ColumnGroup, ExtendedColDef } from '../../types';

/**
 * Reset group drop indicators
 * @returns Empty object for group drop indicators
 */
export const resetGroupDropIndicators = () => {
  return {};
};

/**
 * Calculate column drop index
 * @param columnIndex Column index
 * @returns Calculated drop index for a column
 */
export const calculateColumnDropIndex = (columnIndex: number): number => {
  return columnIndex;
};